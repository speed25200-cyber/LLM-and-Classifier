Donnees vendues depuis https://github.com/themsquared/jev-benchmark (voir LICENSE ci-joint) :
`tasks.jsonl` (60 cas etiquetes a la main : readonly / destructive / privileged / exfiltration ;
34 clear, 14 ambiguous, 12 adversarial) et les sorties brutes de Jev enregistrees le 17 septembre 2026
(`jev-jev-latest.jsonl`, `jev-jev-preview.jsonl`). Elles permettent de comparer le clone et Jev
avec le meme code de mesure (`eval/jev_benchmark.py`).
